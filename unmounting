import {useEffect,useState} from "react"; 
function Child(){ 
  return <h2>I am child</h2> 
} 
function App() { 
     
     const [show,setShow] = useState(false); 
     return( 
        <div> 
          <button onClick={()=>setShow(!show)}>Toggle Login</button> 
{show && <Child/>} 
</div> 
); 
} 
export default App;
